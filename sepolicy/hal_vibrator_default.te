# Vibrator sysfs access is handled by AOSP hal_vibrator.te via sysfs_vibrator.
# genfscon labels /sys/devices/virtual/timed_output/vibrator/enable as sysfs_vibrator.
# Need sysfs_virtual:dir search to navigate to the vibrator path.
allow hal_vibrator_default sysfs_virtual:dir search;
